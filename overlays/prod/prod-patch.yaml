apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: project-http-rt
spec:
  parentRefs:
    - name: prod-project-gw
  rules:
    - backendRefs:
        - name: prod-todo-app-service
          port: 81
    - backendRefs:
        - name: prod-todo-app-service
          port: 80

---
